# Review: SQ-0003 — Bootstrap Lean and Mathlib project

## Review subject

- Author/integration manager: primary implementation manager (`/root`)
- Reviewed base: `d32c50adaec62543e1a7fbc52f62e33ce8f373bb`
- State-transition commit: `9fb933e0cdc4a2e4ffa4167668b778f724e92774`
- Formal/adversarial candidate: `efc31dd522d00db2443d006a2f2df6a22ee7c19d`
- Repeated source-build evidence: `ec047e858f63c663e480952325dab7f3896e64c4`
- Hosted-CI candidate: `11c6f64635c04e23809627a1032417e3e21e5601`
- Review package: to be recorded after this review and handoff are committed
- Review date: 2026-08-08

The reviewed compatibility pair is indivisible:

- Lean channel: `leanprover/lean4:v4.32.2`
- Lean source: `f3b06c705e6c85f5314019d5d3baab0fec5b580c`
- Lake: `5.0.0-src+f3b06c7`
- Mathlib: `905b95818eb32af7874a58b427f50c1711a5e96c`

Exact subject bindings:

| Subject | SHA-256 or commit |
|---|---|
| Lake-generated manifest | `c7e814b11c0e33ec8dd4e58bb31ea0999910bdb32848770dd5721f43eee7a14b` |
| Actual axiom report | `96a2b221dcc7f0a08607fbc935f31c5c4846486b0d4edcb585bddfad2298346b` |
| Trust scanner | `b9c9ea438621b4d63129a43299b3a8f90eafff8ba311b17efa37609ea51276fc` |
| Mutation result | `3040689cf244215b4426b9d9f0f3235c1bb4eab7690327517aba318debea89e7` |
| Workflow | `3acfe5df1f65f1dc706fc23ad78113591073860c5a69a465ee24bc8bbe5390e9`; commit `11c6f64635c04e23809627a1032417e3e21e5601` |
| Project/trust file manifest | `e13681f1a4f838ad56482fa058b85cf0c388959eed380c23c4818bdeab91085a` |
| Mutation-fixture file manifest | `f1ca016cd5e93df6eeceacd7c5b07f23b1ea70cbb49ead59cc5dcd7a9cd96820` |

The two file-manifest digests are the SHA-256 of the byte output of these
sorted Git tree queries at the hosted-CI candidate:

```bash
git ls-tree -r --full-tree 11c6f64635c04e23809627a1032417e3e21e5601 -- \
  lean/StatQED.lean lean/StatQED lean/Examples lean/Tests/AxiomReport.lean \
  lean/tools lean/lakefile.toml lean/lean-toolchain lean/lake-manifest.json \
  scripts/check_lean_trust.py .github/workflows/lean.yml | sha256sum
git ls-tree -r --full-tree 11c6f64635c04e23809627a1032417e3e21e5601 -- \
  lean/Tests/Trust | sha256sum
```

## Independent reviewers and dispositions

| Scope | Reviewer | Independence | Disposition |
|---|---|---|---|
| Mathlib/source lineage | `/root/sq0003_mathlib_scout` | Distinct read-only source/Mathlib scout | APPROVE `ec047e8` |
| Lean build and Lake lock | `/root/sq0003_build_engineer` | Distinct read-only Lean build engineer | APPROVE `11c6f64` |
| Formal declarations and axiom report | `/root/sq0003_formal_trust` | Distinct read-only formal/trust reviewer | APPROVE `ec047e8` |
| Trust mutations and overclaim resistance | `/root/sq0003_adversarial` | Distinct read-only adversarial reviewer | APPROVE `ec047e8` |
| CI and reproducibility | `/root/sq0003_ci_repro` | Distinct read-only CI/reproducibility reviewer | APPROVE `11c6f64` |
| Integration | `/root/sq0003_integrator` | Distinct read-only integration reviewer | Pending exact review package |

No implementation author is the sole formal, adversarial, CI, or integration
reviewer. Later commits from `ec047e8` through `11c6f64` changed only repeated
source-build evidence, the hosted workflow, and a retained hosted failure; they
did not change the reviewed Lean declarations, reporter, scanner, fixtures,
toolchain, Lake configuration, or manifest.

## A. Mathlib/source review

APPROVE. The scout confirmed the official Lean release and source commit, the
full Mathlib commit, the Mathlib `lean-toolchain` body, and the bundled Lake
identity. The toolchain body has SHA-256
`2bdc48adfa58d0017e538a0ad117c5d73d35deec879978f909406a80c8037273`
and is exactly `leanprover/lean4:v4.32.2` with a final newline. The production
project uses the narrow `Mathlib.Data.Set.Defs` import; no project abstraction
or upstream lemma was needed.

Primary sources were retrieved on 2026-08-08 from the official Lean v4.32.2
release and source commit, the exact Mathlib commit, the Lean axiom reference,
and the Lake reference. Lean/Mathlib/Lake are Apache-2.0; the reviewed Elan
bootstrap is MIT OR Apache-2.0. Licenses and repository activity are project
inputs, not proof or security guarantees.

The source reviewer also confirmed the repeated local no-cache run used both
`MATHLIB_NO_CACHE_ON_UPDATE=1` and `LAKE_NO_CACHE=1`, had no existing `.lake`,
did not inherit the host environment or home, resolved Mathlib at the reviewed
commit, built from source, and checked the corrected report.

## B. Lean build and Lake-lock review

APPROVE. The independent build engineer regenerated the exact lock
byte-identically, built the 88-job project, ran the trust-zero smoke, repeated
the exact-candidate isolated source path, reran all 33 mutations, and passed
repository checks from a clean clone without modifying tracked files.

The production layout is the contracted minimum: one `StatQED` library, one
internal definitionally trivial smoke theorem, one example, one environment
report probe, and trust-only fixtures. There is no statistical ontology,
theorem-registry interface, artifact representation, certificate checker, or
public theorem.

Lake generated `lean/lake-manifest.json`; all resolved revisions are immutable,
and Mathlib's input and resolved revisions equal the reviewed full commit. A
fresh isolated `lake update --keep-toolchain` generated byte-identical output
in 36.84 seconds. The normal project-clean build completed 91 jobs in 21.21
seconds, and the smoke example exited zero under `--trust=0`.

The independently isolated local source build against `efc31dd` completed 88
jobs in 60.16 seconds with maximum RSS 966,016 KiB. Its exact environment,
cache controls, timestamps, network boundary, checkout head, report digest, and
command results are retained in `lean/evidence/no-cache-source-build.json`.
The earlier successful source run is retained as superseded evidence rather
than overwritten.

## C. Formal declaration and axiom-report review

APPROVE after one correction round. The first formal review rejected the
earlier candidate because positive controls did not compile, unsafe declaration
status was not taken from `Lean.Environment`, and mutations were not coupled to
live report generation. The corrected candidate closes all three findings.

`Tests/AxiomReport.lean` enumerates declarations owned by the imported StatQED
modules, obtains declaration kind, module, stable diagnostic type
representation, transitive axioms, and `ConstantInfo.isUnsafe` from the live
environment, and rejects project axioms, unsafe declarations, `sorryAx`, and
reviewed native-trust axioms in a project closure. The Python helper binds the
observation to the exact toolchain, Mathlib checkout, Lake manifest digest, and
probe command.

The committed report observes:

- `StatQED.Internal.testOnlySmoke : True`: project theorem, `unsafe: false`,
  no transitive axioms;
- `Set.ext`: imported Mathlib theorem, `unsafe: false`, transitive axioms
  `Quot.sound` and `propext`.

This is an observation in one locked environment, not permission to add
axioms, theorem identity, authorization, source fidelity, or RFC-0005
acceptance.

## D. Trust and mutation review

APPROVE. All 33 permanent cases passed and the regenerated result is
byte-identical to the committed evidence. The suite rejects `sorry`, `admit`,
direct and quoted `sorryAx`, project `axiom`, a checked bodyless `.axiomDecl`,
annotated and quoted axiom constructions, changed toolchain, changed Mathlib
configuration, stale manifest, mutable Mathlib branch/tag, adjacent pair,
missing/fabricated report, unsafe project declarations, and direct or indirect
native-trust shortcuts.

The live mutations first build successfully and then cause actual report
generation to reject `sorryAx`, project axioms, unsafe declarations, and an
indirect `Lean.trustCompiler` dependency. Comment/string and inert-name
positive controls compile and pass. The pinned Lean parser has no `constant`
surface command, so a retained parser failure documents that fact while the
valid `.axiomDecl` control tests the intended bodyless-assumption risk.

The official Lean issue #14576 projection reproducer fails under the pinned
kernel at `--trust=0` with `(kernel) invalid projection`. Ancestry inspection
shows the release commit includes the #14577 fix backport. This regression is
narrow evidence only; it is not a general kernel-security claim.

## E. CI, reproducibility, licensing, and supply-chain review

APPROVE. The independent reviewer reproduced the local workflow structural
checks, inspected the actual hosted logs and permissions, and found no CI or
reproducibility blocker on `11c6f64`.

The workflow has top-level `contents: read`, no persisted checkout credentials,
timeouts and concurrency, exact toolchain identities, and exact action commits:

- `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1`
- `actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97`
- `actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9`

The cache has no prefix fallback and is keyed by runner OS/architecture plus
the exact toolchain and manifest. Repository guardrails and static lock checks
run before cache restoration. The source job is selected for manual, weekly,
or lock-changing runs, uses both cache-disable variables, and has a documented
14-day fail-closed revalidation age.

GitHub Actions run
[`31278943254`](https://github.com/lsablica/statqed/actions/runs/31278943254)
passed on the PR merge of `11c6f64`: repository gates, manifest regeneration,
cached build, smoke, human and JSON trust scans, live report check, all
mutations, and a separate source build. Guardrail run
[`31278943249`](https://github.com/lsablica/statqed/actions/runs/31278943249)
also passed. The observed runner was runner 2.336.0, Ubuntu 24.04.4, image
`ubuntu-24.04` version `20260720.247.2`, Linux `6.17.0-1020-azure`, x86-64,
with Python 3.14.7. The cached job had a genuine cache miss; the separately
selected source job used no `actions/cache` step. This is an observed mutable
hosted image, not immutable Linux or cross-platform support.

An initial workflow had invalid job-expression placement; a later run exposed
that root-level `lean --version` could not select the local channel. Both
failures are retained. The final workflow validates identities with explicit
`elan run`, while Lake commands execute under `lean/lean-toolchain` in the
project directory. The workflow was also checked locally with official
`actionlint` v1.7.12.

## F. Integration review

Pending independent review of the exact review package, clean commands,
allowed-path diff, Draft RFC state, SQ-0004 state, and proposed atomic DONE
transition. No transition is authorized by this draft section alone.

## Commands and results

The exact reviewed commands passed on the local or hosted candidate:

```text
make check
  PASS — repository guardrails; SQ-0002 verifier: 75 probes, 6 recommendations
make list-work
  PASS — DONE=2 IN_PROGRESS=1 READY=1 BLOCKED=56; SQ-0003 active, SQ-0004 ready
git diff --check
  PASS — no output
cd lean && lake build
  PASS — 88 jobs on retained source state; 91 jobs on the normal clean build
lake env lean --trust=0 Examples/Smoke.lean
  PASS
python3 tools/axiom_report.py --check Reports/axioms.json
  PASS
python3 scripts/check_lean_trust.py
python3 scripts/check_lean_trust.py --json
  PASS — no findings
python3 scripts/check_lean_trust.py --run-mutations --json
  PASS — 33/33 cases
actionlint .github/workflows/lean.yml
  PASS — no output
```

Manifest regeneration and the isolated local and hosted no-binary-cache source
builds also passed. The exact retained failure evidence remains under
`lean/evidence/failures/` and was not normalized into success.

## Remaining limitations

- Direct evidence covers only the named local Ubuntu x86-64 host and the
  observed hosted Ubuntu x86-64 image; macOS, Windows, ARM, musl, and other
  runner images are untested.
- The actual report is non-normative environment evidence. RFC-0005 remains
  Draft and no theorem identity, authorization, compatibility, or artifact
  proof lock is defined.
- Caches, networks, Elan, Lake, Git, Python orchestration, GitHub Actions,
  compilers, reviewers, and agents remain operational/evidence inputs. They are
  not scientific or artifact-verification authorities.
- Successful elaboration, build, kernel checking, and axiom observation do not
  establish source-theorem fidelity, statistical validity, external premises,
  numerical correctness, provenance truth, interpretation, artifact-byte
  binding, registry authority, or certificate-checker soundness.
