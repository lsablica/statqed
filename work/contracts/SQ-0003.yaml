{
  "id": "SQ-0003",
  "title": "Bootstrap Lean and Mathlib project",
  "status": "DONE",
  "plan": "docs/exec-plans/active/0001-foundation-bootstrap.md",
  "milestone": "Milestone B — Pin and bootstrap toolchains",
  "objective": "A clean checkout builds and tests a minimal production Lean project using the exact reviewed Lean 4.32.2/Mathlib revision, emits an actual machine-readable axiom report for named declarations, rejects trusted-path mutations, and documents its limited proof-backend claim without introducing statistical semantics.",
  "dependencies": ["SQ-0002"],
  "dependency_evidence": [
    "SQ-0002 is DONE in work/backlog.yaml",
    "SQ-0002 final evidence packaging is merged on main at 01c5b6e1bfacf332dbb01259aa19258a3edd0f9e",
    "work/status.yaml and the shared readiness calculation identify SQ-0003 and SQ-0004 as READY",
    "work/reviews/SQ-0002.md and work/handoffs/SQ-0002.md approve the exact Lean/Mathlib/Lake recommendation and retained normal/no-cache evidence",
    "docs/implementation/toolchain-compatibility.md records Lean 4.32.2 commit f3b06c705e6c85f5314019d5d3baab0fec5b580c, Mathlib commit 905b95818eb32af7874a58b427f50c1711a5e96c, and Lake 5.0.0-src+f3b06c7 as one exact pair"
  ],
  "allowed_paths": [
    "work/contracts/SQ-0003.yaml",
    "work/contracts/SQ-0008.yaml",
    "lean/**",
    "scripts/check_lean_trust.py",
    ".github/workflows/lean.yml",
    "docs/implementation/lean-core.md",
    "docs/quality/dashboard.md",
    "docs/exec-plans/active/0001-foundation-bootstrap.md",
    "work/backlog.yaml",
    "work/status.yaml",
    "work/reviews/SQ-0003.md",
    "work/handoffs/SQ-0003.md"
  ],
  "forbidden_paths": [
    "backend/**",
    "frontends/**",
    "schemas/**",
    "conformance/**",
    "theorem-registry/**",
    "methods/**",
    "generated/**",
    "rfcs/**",
    "docs/adr/**",
    "docs/design/**",
    "docs/spec/**",
    "scripts/bootstrap/run_toolchain_probes.py",
    "docs/research/toolchain-prototypes/**",
    ".github/workflows/rust.yml",
    "Makefile"
  ],
  "public_changes": [
    "Initial Experimental Lean project layout, build commands, trust-scan format, actual axiom-report format, and Lean-specific CI surface only",
    "No statistical definition, theorem-registry identity, artifact verification interface, certificate checker, or public statistical theorem is created by this task"
  ],
  "accepted_decisions": [
    "ADR-0001 — foundation-phase monorepo",
    "ADR-0002 — Lean 4 with pinned Mathlib as the initial normative proof backend",
    "ADR-0008 — mode-specific minimal TCB and conditionally untrusted producers",
    "ADR-0011 — data-free, test-only structural foundation slice",
    "SQ-0002 reviewed recommendation — exact Lean 4.32.2/Mathlib 905b95818eb32af7874a58b427f50c1711a5e96c pair"
  ],
  "sources": [
    "docs/implementation/toolchain-compatibility.md and the content-addressed SQ-0002 Lean prototype evidence",
    "Official Lean v4.32.2 release, Lean reference manual, Lake documentation, axiom validation documentation, and source repository",
    "Exact Mathlib commit 905b95818eb32af7874a58b427f50c1711a5e96c, its lean-toolchain body, lake manifest behavior, cache guidance, and contribution conventions",
    "Official GitHub Actions documentation and exact action source commits used by the Lean workflow"
  ],
  "assumptions": [
    "SQ-0003 initializes only the proof-backend project and does not settle any Draft RFC",
    "The exact reviewed Lean/Mathlib pair is the default production pin; changing it requires a reproduced blocker, a contract/plan revision, and independent re-review rather than silent substitution",
    "A successful build establishes build compatibility only for the named environment and locked sources",
    "Axiom reporting describes the actual named declarations relative to the locked environment; it does not establish source fidelity, external premises, artifact-byte binding, or statistical validity",
    "The test-only smoke proposition is not a public StatQED theorem and cannot satisfy a public-theorem non-vacuity requirement",
    "GitHub hosted-runner execution is operational evidence for the observed image, not immutable cross-platform compatibility evidence"
  ],
  "steps": [
    "Run git status, git rev-parse HEAD, make check, make list-work, and git diff --check; confirm SQ-0003 and SQ-0004 are READY and no task is active",
    "Create agent/SQ-0003-lean-bootstrap and atomically mark only SQ-0003 IN_PROGRESS in its contract, backlog, status, and Plan 0001 Progress section; leave SQ-0004 READY",
    "Inspect the SQ-0002 Lean recommendation, exact manifests, normal build, no-binary-cache build, mismatch mutations, and axiom evidence before writing production files",
    "Create lean/lean-toolchain for leanprover/lean4:v4.32.2 and a minimal Lake project whose Mathlib dependency is locked to full commit 905b95818eb32af7874a58b427f50c1711a5e96c",
    "Generate and commit the production lake-manifest.json from the exact configuration; verify regeneration is byte-identical and that an altered toolchain or Mathlib revision fails the lock check",
    "Create the smallest maintainable StatQED namespace/library layout, one internal smoke module importing a relevant pinned Mathlib module, and one test/example entry point; add no statistical ontology or inference declaration",
    "Keep the smoke proposition definitionally trivial and internal/test-only; document that it is not a registered theorem, semantic non-vacuity witness, artifact claim, or statistical result",
    "Implement scripts/check_lean_trust.py to inspect only the intended project/trusted paths, reject sorry/admit/sorryAx/project-defined axiom and unsafe trust shortcuts, validate the exact toolchain/Mathlib lock, and emit deterministic machine-readable findings",
    "Create a deterministic actual axiom report for explicitly named project and imported smoke declarations, including the locked Lean/Mathlib environment and the transitive axiom set rather than a hand-written allowlist assertion",
    "Run both a normal/cache-enabled clean build and an independently isolated no-binary-cache source build; record exact commands, environment, duration, cache/network state, and results",
    "Add negative mutations for sorry, admit, sorryAx/project axiom, altered lean-toolchain, altered Mathlib revision or manifest, and any native/compiled shortcut whose result would be trusted without an accepted boundary; demonstrate fail-closed behavior",
    "Add .github/workflows/lean.yml with least-privilege permissions, pinned action revisions, the exact Lean/Mathlib pair, repository guardrails, build/test/trust/axiom checks, and a clean-checkout path; record runner-image metadata without claiming immutable platform support",
    "Update docs/implementation/lean-core.md with exact installation, build, test, no-cache, trust-scan, axiom-report, CI, update, rollback, and limitation instructions",
    "Obtain distinct Mathlib/source, Lean/formal, trust/adversarial, CI/reproducibility, and integration reviews of exact commits and generated locks",
    "Create work/reviews/SQ-0003.md and work/handoffs/SQ-0003.md, run all commands from a clean checkout, then atomically transition SQ-0003 to DONE while leaving SQ-0004 state unchanged and recomputing the READY set"
  ],
  "tests": [
    "make check and make list-work before, during, and after task transitions",
    "git diff --check",
    "cd lean && lake build",
    "Run the project smoke test/example through lake or lake env lean using documented exact commands",
    "Regenerate lake-manifest.json in an isolated clean directory and compare it byte-for-byte with the committed lock",
    "Repeat the exact build with Mathlib binary-cache use disabled and isolated HOME/cache directories",
    "python3 scripts/check_lean_trust.py in human-readable and machine-readable modes",
    "Actual axiom report for every named task declaration, checked against the locked environment and retained as review evidence",
    "Mutations containing sorry, admit, sorryAx, a project-defined axiom, an altered toolchain, an altered Mathlib revision/manifest, and an unreviewed native-trust shortcut are rejected for the intended reason",
    "A mismatched adjacent Lean/Mathlib pair is rejected or fails reproduction rather than being normalized to the reviewed pair",
    "Lean workflow syntax and commands are reproduced locally where applicable; the resulting GitHub Actions run must complete successfully before final integration",
    "No files outside allowed paths and no statistical definitions, schemas, registry records, artifact semantics, or Draft-RFC acceptance"
  ],
  "reviewers": {
    "semantic_source": [
      "Mathlib/source curator — exact release, commit, toolchain-body, lock, cache, and documentation lineage"
    ],
    "formal_conformance": [
      "Lean build engineer — Lake layout, exact dependency lock, normal/no-cache reproducibility",
      "independent Lean/formal reviewer — declarations, actual axiom report, namespace/import quality, and absence of hidden trust"
    ],
    "adversarial": [
      "trust mutation reviewer — sorry/admit/sorryAx/axiom/toolchain/manifest/native-shortcut fail-closed tests",
      "CI/release reviewer — action pinning, permissions, cache isolation, clean-checkout behavior, and claim boundaries"
    ],
    "integrator": [
      "independent integration reviewer — contract scope, generated-lock provenance, exact commands, workflow result, status transition, and successor readiness"
    ]
  },
  "acceptance": [
    "lean/ contains a minimal maintainable production Lean project pinned to Lean 4.32.2 and exact Mathlib commit 905b95818eb32af7874a58b427f50c1711a5e96c",
    "The committed lake manifest is reproducibly generated and byte-identical on clean regeneration",
    "Normal and isolated no-binary-cache builds pass from clean state with exact evidence",
    "The project contains no statistical ontology, inference theorem, artifact checker, theorem-registry semantics, or public theorem claim",
    "The smoke declaration is explicitly internal/test-only and is not represented as scientific or non-vacuity evidence",
    "The trust scanner rejects all required mutations and does not mistake imported Mathlib axioms for project-defined axioms",
    "The actual axiom report names exact declarations, exact locked environment, and actual transitive axioms; it is not a permission list for adding axioms",
    "No sorry, admit, sorryAx, project-defined axiom, or unreviewed native/compiled trust shortcut exists in the accepted project path",
    "The Lean workflow uses least privilege and pinned action revisions, runs repository and Lean-specific gates, and succeeds on the integrated commit or reviewed PR head",
    "Documentation gives exact local/clean/no-cache commands, update/rollback procedure, TCB limitations, and nonclaims",
    "Independent source, formal, adversarial, CI, and integration reviews approve the exact subject",
    "All required commands pass, SQ-0003 becomes DONE, SQ-0004 remains correctly READY or active only if a separate execution has legitimately begun, and the repository computes the correct next READY set"
  ],
  "commands": [
    "make check",
    "make list-work",
    "git diff --check",
    "cd lean && lake build",
    "python3 scripts/check_lean_trust.py"
  ],
  "handoff_outputs": [
    "Pinned lean/lean-toolchain, lakefile, and reproducible lake manifest",
    "Minimal Lean namespace, smoke module, and test/example",
    "scripts/check_lean_trust.py with mutation evidence",
    "Actual machine-readable axiom report and exact environment lock evidence",
    "Normal and no-binary-cache clean-build transcripts",
    "Successful least-privilege Lean CI run and runner metadata",
    "Updated docs/implementation/lean-core.md",
    "Independent source/formal/adversarial/CI/integration review record",
    "Updated active plan, backlog, status, dashboard, and work/handoffs/SQ-0003.md"
  ]
}
