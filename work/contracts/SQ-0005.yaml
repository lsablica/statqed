{
  "id": "SQ-0005",
  "title": "Prototype deterministic serialization",
  "status": "DONE",
  "plan": "docs/exec-plans/active/0001-foundation-bootstrap.md",
  "milestone": "Milestone C — Settle an encoding prototype",
  "objective": "Select, specify, and independently reproduce the smallest data-free StatQED deterministic encoding profile; accept RFC-0001 only after byte-level, decoder, duplicate-key, numeric, Unicode, extension, domain-separation, malformed-input, and resource evidence passes independent review, while leaving RFC-0006 logical-data semantics untouched for SQ-0027.",
  "dependencies": ["SQ-0002", "SQ-0004"],
  "dependency_evidence": [
    "SQ-0002 is DONE and preserves current CBOR/CDDL/Arrow compatibility probes, failures, source records, and experimental-library boundaries",
    "SQ-0004 is DONE and its final post-merge evidence record is on main at 4aa0b9c145ce2595f3630d17abcfb7e4248579b4",
    "SQ-0004 provides Rust 1.97.1 development/acquisition and a Rust 1.85.1 locked offline floor but introduces no encoding semantics",
    "work/status.yaml and the shared readiness calculation identify SQ-0005 and SQ-0008 as READY with no active task",
    "The decision register assigns RFC-0001 to SQ-0005 and RFC-0006 to SQ-0027",
    "SQ-0006 remains blocked on SQ-0005 completion and RFC-0001 acceptance"
  ],
  "allowed_paths": [
    "work/contracts/SQ-0005.yaml",
    "work/contracts/SQ-0006.yaml",
    "ARCHITECTURE.md",
    "schemas/prototypes/**",
    "conformance/prototypes/**",
    "docs/spec/canonicalization.md",
    "docs/research/serialization/**",
    "source-audits/encoding/**",
    "scripts/serialization/**",
    ".github/workflows/serialization-prototypes.yml",
    "Makefile",
    "rfcs/0001-deterministic-encoding.md",
    "docs/adr/0004-deterministic-cbor-cddl.md",
    "docs/quality/dashboard.md",
    "docs/exec-plans/active/0001-foundation-bootstrap.md",
    "work/backlog.yaml",
    "work/status.yaml",
    "work/reviews/SQ-0005.md",
    "work/handoffs/SQ-0005.md"
  ],
  "forbidden_paths": [
    "backend/**",
    "lean/**",
    "frontends/**",
    "schemas/v0/**",
    "conformance/golden/**",
    "theorem-registry/**",
    "methods/**",
    "generated/**",
    "rfcs/0002-evidence-taxonomy.md",
    "rfcs/0003-artifact-decoder-trust.md",
    "rfcs/0004-core-statistical-ontology.md",
    "rfcs/0005-theorem-identity-and-compatibility.md",
    "rfcs/0006-canonical-logical-data-digest.md",
    "rfcs/0007-compatibility-and-migration.md",
    "rfcs/0008-artifact-envelope-and-offline-resolution.md",
    "rfcs/0009-community-governance-structure.md",
    "docs/adr/0001-monorepo-first.md",
    "docs/adr/0002-lean4-normative-proof-backend.md",
    "docs/adr/0003-rust-reference-backend.md",
    "docs/adr/0005-typed-assurance-graph.md",
    "docs/adr/0006-thin-frontends.md",
    "docs/adr/0007-versioned-theorem-registry.md",
    "docs/adr/0008-minimal-tcb-and-untrusted-producers.md",
    ".github/workflows/lean.yml",
    ".github/workflows/rust.yml"
  ],
  "public_changes": [
    "RFC-0001 deterministic normative encoding profile, if and only if the complete reviewed validation plan passes",
    "ADR-0004 disposition and wording, if and only if it exactly matches an Accepted RFC-0001",
    "Draft canonicalization specification for the data-free foundation object classes",
    "No logical-data model, physical-to-logical lowering, or RFC-0006 data digest is created by this task",
    "Post-acceptance integration maintenance may update only the two stale ARCHITECTURE.md current-state sentences that still call Accepted RFC-0001 a candidate; it may not change architecture, package boundaries, trust claims, or the marked normative scope"
  ],
  "accepted_decisions": [
    "ADR-0001 — foundation-phase monorepo",
    "ADR-0003 — Rust is a reference operational backend and not the semantic authority",
    "ADR-0006 — thin language frontends compile to shared semantics",
    "ADR-0008 — mode-specific minimal TCB and conditionally untrusted producers",
    "ADR-0011 — data-free, test-only foundation_structural slice",
    "SQ-0002 — deterministic CBOR/CDDL and relevant libraries remain Experimental compatibility evidence until RFC-0001 is resolved",
    "SQ-0004 — production Rust workspace remains dependency-free and must not be modified by serialization prototypes"
  ],
  "sources": [
    "RFC 8949, including preferred serialization, core and length-first deterministic encoding, well-formedness, validity, duplicate map keys, map-key equivalence, tag validity, and security considerations",
    "RFC 8610, RFC 9165, RFC 9682, and RFC 9741, including current errata and exact published CDDL grammar/control-operator boundaries",
    "Current IANA CBOR Tags registry and every tag specification selected or explicitly excluded by the profile",
    "Unicode Standard Annex #15 at one exact Unicode version if normalization is selected, plus UTF-8 conformance sources",
    "Current CDDL module/import Internet-Draft only as a Work in Progress input; it is not an accepted standards feature",
    "Cryptographic primary sources and registries for every selected hash algorithm, algorithm identifier, output length, and domain-separation construction",
    "SQ-0002 cbor2/ciborium/minicbor/cddl/Arrow prototype evidence and retained divergent or malformed cases",
    "Official language/package documentation and exact dependency/license/advisory sources for every isolated prototype implementation"
  ],
  "assumptions": [
    "SQ-0005 governs only the deterministic encoding of data-free normative object classes needed by the foundation slice",
    "RFC-0001 is owned by SQ-0005; SQ-0005 cannot become DONE while RFC-0001 remains Draft unless ownership is transferred through a separately reviewed planning change",
    "RFC-0006 is owned by SQ-0027 and is read-only in SQ-0005; logical tables, Arrow lowering, logical-data identity, privacy properties, and canonical logical-data digests are out of scope",
    "Generic atom and domain-framing findings relevant to later data work are recorded outside RFC-0006 without claiming a logical-data decision",
    "CDDL constrains accepted structure but does not by itself establish canonical bytes, duplicate behavior, semantic normalization, digest identity, or statistical validity",
    "Independent implementations must not call each other, consume each other's generated bytes as their oracle, or share the same canonicalizer lineage",
    "Prototype success does not make prototype libraries part of a production TCB or authorize changes to the production Rust backend",
    "A digest comparison is conditional on the selected algorithm and framing assumptions and is not proof of collision-free identity or provenance truth",
    "All direct compatibility and performance claims are limited to actually executed platforms and recorded environments",
    "SQ-0008 is independently READY and must remain unstarted and unmodified in the SQ-0005 branch",
    "Completing SQ-0005 with RFC-0001 Accepted is expected to make SQ-0006 dependency- and decision-ready; only readiness-state records for SQ-0006 may change during the final transition"
  ],
  "steps": [
    "Run git status, git rev-parse HEAD, make check, make list-work, and git diff --check; confirm SQ-0001 through SQ-0004 are DONE, SQ-0005 and SQ-0008 are READY, and no task is active",
    "Create agent/SQ-0005-deterministic-serialization and atomically mark only SQ-0005 IN_PROGRESS in its contract, backlog, status, and Plan 0001 Progress section; leave SQ-0008 READY",
    "Produce a dated primary-source audit under source-audits/encoding covering RFC 8949, CDDL RFCs and errata, the IANA tag registry, Unicode policy, cryptographic algorithms, current module-draft status, implementation documentation, licenses, and advisories",
    "Define a language-neutral prototype semantic value model before defining bytes; distinguish integer, bignum, rational, decimal fraction, IEEE bit pattern, byte string, text string, array, map entry sequence, boolean, null, interval, extension, and explicitly unsupported values",
    "Create a decision table that settles or excludes deterministic map ordering, preferred lengths, map-key types, duplicate equivalence, Unicode handling, numeric normal forms, float and NaN behavior, tags, extensions, decoder strictness, profile/schema identifiers, and all resource bounds",
    "Define CBOR well-formedness, CBOR validity, application expectedness, CDDL shape conformance, deterministic-profile conformance, semantic validity, and digest verification as separate result classes with stable failure codes",
    "Implement an isolated Rust/library-backed prototype under schemas/prototypes without modifying backend/ or inheriting production semantic authority",
    "Implement a separately originated reference encoder/decoder oracle in another language directly from the selected RFC subset; it must not call the Rust prototype, reuse the same canonicalization library, or treat Rust output as expected truth",
    "Optionally exercise a third diagnostic CBOR implementation to expose library disagreement, while recording implementation lineage and shared dependencies",
    "Ensure at least one decoder path exposes raw ordered map entries before any native map can overwrite or discard duplicates; reject exact and application-equivalent duplicate keys according to the profile",
    "Build a reviewed semantic fixture catalogue covering minimal, boundary, maximal, malformed, invalid, non-profile, unsupported, and resource-exhaustion cases before accepting any golden bytes",
    "Generate independently produced exact CBOR bytes and diagnostic projections for every accepted fixture; compare bytes, decoded semantic values, accept/reject status, and stable failure class",
    "Add a deliberate encoder and decoder divergence and prove the differential harness detects each before golden vectors can be updated",
    "Test core versus length-first map ordering, preferred versus non-preferred integers/lengths/tags/floats, indefinite lengths, invalid UTF-8, Unicode preservation or selected normalization, signed zero, infinities, NaN payloads, bignum leading zeros, zero denominators, non-normal rationals/decimals, reversed intervals, unknown tags, duplicate extension IDs, and unknown critical/noncritical extensions",
    "Define generic data-free digest framing with explicit purpose, algorithm, profile, schema, and framing identifiers; test cross-domain replay, purpose substitution, algorithm/profile downgrade, unsupported fallback, truncation, concatenation ambiguity, and empty-component cases without defining RFC-0006 logical-data semantics",
    "Set and test explicit maximum nesting, collection size, map entries, string/byte length, total input/output, integer magnitude, tag depth, extension count, and diagnostic-output bounds; preserve timeout and allocation evidence",
    "Use only published CDDL syntax in any normative candidate files; pin and label any module/import draft experiment as non-normative Experimental evidence",
    "Record exact prototype toolchains, dependency locks, implementation commits, licenses, advisory observations, commands, environments, platform, outputs, failures, and hashes",
    "Create a standard-library static evidence verifier under scripts/serialization that binds RFC text, source audit, profile specification, semantic fixtures, golden bytes, implementation lineage, results, failures, and reviews; add its static verification to make check",
    "Create a least-privilege, commit-pinned serialization-prototype workflow that runs repository guardrails, independent encoders/decoders, differential tests, malformed/resource tests, evidence verification, dependency/license/advisory gates, and clean regeneration",
    "Revise RFC-0001 from evidence rather than from implementation preference; resolve every validation-plan item, record rejected alternatives and unresolved limits, and obtain source, semantic, interoperability, formal, security, and integration review",
    "If RFC-0001 is approved, mark it Accepted and update ADR-0004 to Accepted with exactly matching scope; otherwise leave RFC-0001 Draft, mark SQ-0005 BLOCKED or IN_REVIEW, and do not transition the task to DONE",
    "Update docs/spec/canonicalization.md and docs/quality/dashboard.md with only the accepted data-free profile, evidence, maturity, limits, TCB, and nonclaims",
    "Create work/reviews/SQ-0005.md and work/handoffs/SQ-0005.md, run all commands from clean checkouts, and obtain independent integration approval",
    "After RFC-0001 acceptance and all gates, atomically transition SQ-0005 to DONE across its contract, backlog, status, and Plan 0001; recompute readiness and change only the SQ-0006 contract/backlog status from BLOCKED to READY if the shared calculation makes it eligible; preserve SQ-0008 READY and unstarted",
    "Run all repository, evidence, prototype, and workflow gates again after the final transition and stop; do not begin SQ-0006 or SQ-0008"
  ],
  "tests": [
    "make check, make list-work, and git diff --check before, during, and after every task-state transition",
    "Primary-source audit records exact versions, retrieval dates, errata status, and Work in Progress versus published-standard status",
    "Independent Rust and non-Rust encoders produce byte-identical output for every accepted semantic fixture without using a shared canonicalizer or generated expected bytes",
    "Independent decoders agree on semantic values and stable result classes for accepted, malformed, invalid, non-profile, and unsupported inputs",
    "The harness detects deliberate byte-level, semantic, duplicate-handling, and accept/reject divergence",
    "Core and length-first map-order vectors differ where expected and the unselected order is rejected or classified exactly as the profile specifies",
    "Exact duplicates and duplicates under the selected application key equivalence are detected before native-map collapse",
    "Preferred/non-preferred integer, length, tag, and float forms are tested at every encoding-width boundary",
    "Invalid UTF-8 and the selected Unicode preservation/normalization cases include composed/decomposed, combining-mark, noncharacter, control, and normalization-collision vectors",
    "Integer, bignum, rational, decimal, IEEE bits, finite float, infinity, signed zero, NaN payload, interval, null, boolean, byte/text, tag, and extension cases are either fully specified and tested or explicitly rejected",
    "Strict rejection versus accepted normalization is tested separately for every non-profile input class; decode/re-encode equality is not substituted for strict validation",
    "Unknown tag, tag-content mismatch, unknown critical/noncritical extension, duplicate extension, and extension-order cases have stable outcomes",
    "Maximum-depth, collection, map, string, bytes, integer, tag, extension, total-input, total-output, timeout, and allocation boundaries pass on both sides",
    "Purpose, algorithm, profile, schema, framing, length, and payload mutations fail across every data-free normative digest domain; cross-domain replay, fallback, downgrade, truncation, and concatenation ambiguity are rejected",
    "CDDL fixtures validate only the selected published-syntax structural subset and are never reported as canonical-byte or semantic proof",
    "Prototype dependencies have exact locks, normalized license inventories, point-in-time advisory evidence, and clean regeneration",
    "The static evidence verifier rejects changed RFC/profile text, source-audit drift, changed vectors, missing failures, shared-lineage claims, golden-byte replacement, review-hash mismatch, and stale status metadata",
    "The serialization-prototype workflow uses least privilege, exact action pins, clean environments, bounded execution, and passes on the reviewed head and main merge",
    "RFC-0006 is byte-identical to main and remains Draft under SQ-0027 ownership",
    "No production backend, Lean, frontend, schema-v0, artifact, registry, certificate, or statistical file changes",
    "Final contract/backlog/status parity is valid; SQ-0006 receives a readiness-only update if and only if RFC-0001 is Accepted and the shared calculation makes it eligible",
    "SQ-0008 remains READY and unstarted"
  ],
  "reviewers": {
    "semantic_source": [
      "CBOR/CDDL/Unicode/cryptography source curator — exact published standards, errata, registries, draft status, and source lineage",
      "encoding-profile and numeric-model architect — semantic atoms, key equivalence, numeric normal forms, Unicode policy, tags, extensions, and exclusions"
    ],
    "formal_conformance": [
      "Rust prototype reviewer — isolated implementation, exact lock, parser behavior, resource bounds, and no production-backend authority",
      "independent reference-oracle reviewer — implementation lineage and direct-from-spec behavior",
      "conformance reviewer — semantic fixtures, byte vectors, failure classes, deliberate divergence, and clean regeneration",
      "formal/trust-boundary reviewer — separation of structural, canonical, semantic, digest, and statistical claims"
    ],
    "adversarial": [
      "parser/security reviewer — malformed CBOR, duplicate collapse, Unicode confusion, type confusion, tags, extensions, resource exhaustion, and implementation differential",
      "cryptographic framing reviewer — domain separation, algorithm agility, downgrade, fallback, truncation, replay, and digest nonclaims",
      "CI/release reviewer — action pins, permissions, dependency locks, license/advisory evidence, reproducibility, and platform-claim limits"
    ],
    "integrator": [
      "independent integration reviewer — exact RFC/profile/vector/evidence subject, reviewer independence, allowed-path scope, static evidence verifier, workflow result, atomic RFC/task transition, SQ-0006 readiness-only update, and SQ-0008 preservation"
    ]
  },
  "acceptance": [
    "RFC-0001 is Accepted and every validation-plan item is resolved, tested, explicitly excluded, or converted to a named blocking limitation before SQ-0005 becomes DONE",
    "ADR-0004 is Accepted only with text that exactly matches the Accepted RFC-0001 scope",
    "A versioned, bounded, data-free deterministic encoding profile defines one accepted byte representation for every accepted semantic fixture and stable rejection classes for all other fixture classes",
    "At least two independently originated canonicalization implementations/oracles agree on every accepted vector and the harness demonstrably rejects divergent implementations",
    "Map duplicate detection occurs before native-map information loss and uses the exact selected key-equivalence rules",
    "Numeric, Unicode, tag, extension, decoder strictness, and resource policies are explicit rather than delegated silently to a library",
    "Generic data-free digest framing is explicit and mutation-tested without defining RFC-0006 logical-data semantics or claiming collision-free identity",
    "CDDL is used only for its selected published structural language and is not reported as canonical-byte, semantic, inferential, or kernel verification",
    "Prototype dependencies, sources, licenses, advisories, commands, platforms, failures, vectors, and reviews are content-addressed and reproducible",
    "The static evidence verifier is part of make check and fails on any reviewed evidence or status drift",
    "The serialization-prototype workflow passes all independent, differential, malformed, resource, dependency, security, and regeneration gates on the reviewed head and main merge",
    "No production backend, Lean, frontend, schema-v0, artifact, registry, certificate, logical-data, or statistical semantics are introduced",
    "RFC-0006 remains Draft and unchanged under SQ-0027 ownership",
    "Independent source, semantic, implementation, conformance, security, cryptographic, CI, formal, and integration reviews approve the exact subject",
    "All required commands pass, SQ-0005 becomes DONE, SQ-0001 through SQ-0004 remain DONE, and SQ-0008 remains READY/unstarted",
    "The repository computes the next READY set; if no reviewed blocker changes the DAG, SQ-0006 and SQ-0008 are READY, and neither is begun in this execution"
  ],
  "commands": [
    "make check",
    "make list-work",
    "git diff --check",
    "python3 scripts/serialization/check_evidence.py",
    "python3 scripts/serialization/run_conformance.py",
    "Exact Rust prototype format, lint, build, test, and dependency commands documented by the implementation",
    "Exact independent-oracle test and packaging commands documented by the implementation",
    "Exact malformed/resource/differential and clean-regeneration commands documented by the implementation"
  ],
  "handoff_outputs": [
    "Accepted RFC-0001 and matching ADR-0004, or an explicit BLOCKED handoff if acceptance evidence is insufficient",
    "Dated primary-source audit for CBOR, CDDL, Unicode, tags, cryptography, and implementations",
    "Versioned data-free semantic value model and deterministic encoding profile",
    "Isolated Rust prototype and independent non-Rust reference oracle with implementation-lineage record",
    "Reviewed semantic fixtures, exact golden bytes, stable result classes, and deliberate-divergence evidence",
    "Duplicate-key, numeric, Unicode, tag, extension, strictness, malformed, and resource-limit corpus",
    "Generic data-free digest framing and cross-domain/downgrade/truncation mutation corpus",
    "CDDL structural prototype restricted to selected published syntax",
    "Prototype dependency locks, license inventories, advisory observations, command logs, failures, and platform records",
    "Standard-library static evidence verifier integrated into make check",
    "Successful least-privilege serialization-prototype workflow",
    "Updated canonicalization specification and Experimental/Candidate quality evidence without overclaim",
    "Independent source/semantic/implementation/conformance/security/cryptographic/CI/formal/integration review record",
    "Atomic SQ-0005 DONE transition and readiness-only SQ-0006 contract/backlog update when computed eligible",
    "Updated active plan, status, dashboard, and work/handoffs/SQ-0005.md"
  ]
}
