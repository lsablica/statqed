{
  "id":"SQ-0007","title":"Create theorem registry and lock v0","status":"BLOCKED","plan":"docs/exec-plans/active/0001-foundation-bootstrap.md","objective":"After RFC-0005 acceptance, the ADR-0011 test-only `True` conformance record has separate semantic/environment/registry/proof/axiom locks and a deterministic offline resolver without being presented as a public theorem or non-vacuity witness.",
  "dependencies":["SQ-0003","SQ-0006"],
  "allowed_paths":["theorem-registry/**","schemas/registry/**","lean/StatQED/Registry/**","backend/crates/statqed-registry/**","docs/design/theorem-registry.md","docs/spec/theorem-lock.md","rfcs/0005-theorem-identity-and-compatibility.md","docs/adr/0007-versioned-theorem-registry.md","work/status.yaml","docs/exec-plans/active/0001-foundation-bootstrap.md"],
  "forbidden_paths":["lean/StatQED/Experiment/**","methods/**","frontends/**"],
  "public_changes":["theorem ID and statement normalization v0"],
  "steps":["Accept RFC-0005 exact normalizer, bounded meaning-bearing dependency/environment closure, canonical registry record, verifier-selected authorization root/policy, digest/domain profile, proof/build lock, revocation/history rules, and axiom policy","Define the test-only, definitionally trivial `True` conformance record and controlled metadata; prohibit public-theorem or non-vacuity claims for it","Canonicalize the elaborated proposition and environment independently","Implement lock schema and deterministic offline Rust resolver","Reject name/digest/metadata-only, artifact-supplied-root, revoked-root, and wrong-direction substitutions","Add checked compatibility toy examples"],
  "tests":["Proof-body refactor preserves semantic identity only in the same environment but changes proof/build lock and axiom report","Referenced-definition/environment/statement changes alter the appropriate lock","Forbidden-axiom mutation fails with unchanged proposition","Bytes/content claiming `False` cannot be accepted through the `True` record; this fixture is not generalized beyond the exact path","Whole-registry replacement and forged ID/review/maturity metadata fail","Root mismatch and revoked/historical-root policy cases","Closure cycle/width/depth/work-budget failures deterministic","Missing/wrong registry/environment/proof lock fails","Wrong-direction or metadata-only compatibility fails","resolver deterministic, bounded, and offline"],
  "reviewers":{"semantic_source":["source curator"],"formal_conformance":["lean reviewer","conformance reviewer"],"adversarial":["counterexample reviewer","security reviewer"],"integrator":["integration-reviewer"]},
  "acceptance":["RFC-0005 is Accepted","ID/version/canonical-proposition/environment/registry/proof/axiom semantics documented","Statement digest is not treated as equality or source-fidelity proof","Resolver cannot silently substitute meaning","Toy lock resolves offline"],
  "commands":["cd lean && lake build","cd backend && cargo test --workspace","make check"],"handoff_outputs":["Toy registry entry","canonical proposition/environment/registry/proof/axiom vectors","lock fixture"]
}
