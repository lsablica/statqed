{
  "id":"SQ-0010","title":"Define safe artifact envelope","status":"BLOCKED","plan":"docs/exec-plans/active/0001-foundation-bootstrap.md","objective":"A deterministic bounded `.statqed` envelope is specified with hostile-input fixtures and exact failure behavior.",
  "dependencies":["SQ-0006","SQ-0009"],
  "allowed_paths":["schemas/artifact/**","conformance/artifact/**","docs/spec/artifact.md","docs/spec/provenance.md","docs/quality/trust-report-template.md","rfcs/0008-artifact-envelope-and-offline-resolution.md","work/status.yaml","docs/exec-plans/active/0001-foundation-bootstrap.md"],
  "forbidden_paths":["backend/src/**","lean/StatQED/Artifact/**","frontends/**"],
  "public_changes":["artifact envelope v0"],
  "steps":["Accept RFC-0008 exact container, manifest authority, path/name normalization, allowed feature/compression set, resource policy, local-only resolution, and provenance privacy/disclosure rules","Define required entries and one authoritative manifest","Specify deterministic archive order/metadata","Set pre-extraction per-entry/aggregate size, expansion, count, depth, allocation, and work limits","Separate normative and inert report files","Define digest coverage and critical features","Create traversal/link/device/duplicate-manifest/trailing/concatenated/nested/encrypted/oversize/truncation/unknown fixtures","Define allowlisted provenance capture and redaction identity effects","Review offline archival behavior"],
  "tests":["Schema validation","absolute/drive/UNC/separator/dot-dot/NUL/Unicode-alias path rejection","duplicate normalized entry and alternate-manifest rejection","symlink/hardlink/device/ADS/encryption/multipart/nested/unsupported-compression rejection","trailing bytes and concatenated archive behavior","deterministic resource-budget failures before extraction where possible","digest mismatch","non-normative report active-content isolation","no implicit network fetch","secret/token/raw-environment/private-path leak fixtures","committed/normative redaction creates a new normative artifact/result identity","inert-report-only redaction preserves normative artifact identity, changes physical bundle/file commitment, and records disclosure transformation","unresolved leaf is limited to external/uncommitted references or a newly identified normative object/result"],
  "reviewers":{"semantic_source":["interoperability reviewer"],"formal_conformance":["conformance reviewer"],"adversarial":["security reviewer"],"integrator":["integration-reviewer"]},
  "acceptance":["RFC-0008 is Accepted","No bundled code or active report execution path","All limits and failure rules explicit","Artifact can be verified offline with only explicitly supplied local references","Report cannot influence normative result","Provenance is allowlisted/privacy-minimized; committed/normative changes always create new identity, while inert-report redaction preserves normative artifact identity, changes physical bundle bytes/file commitment, and records the disclosure transformation"],
  "commands":["make check"],"handoff_outputs":["Envelope spec","fixture manifest","security review"]
}
