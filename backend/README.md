# Rust Reference Backend

Planned responsibilities: typed schema handling, validation, canonical encoding, logical digests, safe artifact archives, theorem-lock resolution, conformance tooling, and the `statqed` CLI.

The workspace will be initialized by SQ-0004 after toolchain ratification. Rust is a reference implementation, not the normative statistical semantics.
