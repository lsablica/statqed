# Repository Scripts

Current scripts validate the architecture/task scaffold using only Python standard library. Language-specific scripts are added by their bootstrap tasks and documented in scoped `AGENTS.md` files.

```bash
python3 scripts/check_repository.py
python3 scripts/list_work.py
```
