---
name: execute-plan
description: Execute an active StatQED plan using dependency-ready tasks and merge gates.
---

# execute-plan

Read and execute the canonical workflow at `agents/workflows/execute-plan.md`.

Also read:

- root `AGENTS.md`;
- the active task contract;
- the nearest nested `AGENTS.md`;
- all protocols linked by the canonical workflow.

Do not replace the canonical workflow with this wrapper. Report any conflict and stop.
