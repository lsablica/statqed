# Claude Code entry point

Read `AGENTS.md` first. Then read the nearest nested `AGENTS.md` for any directory you modify.

Canonical workflows are in `agents/workflows/`. Project subagents are in `.claude/agents/`. Skills are in `.claude/skills/`.

The active manager contract is `START_HERE.md`. Do not substitute this short file for the project charter, active execution plan, or task contract.
