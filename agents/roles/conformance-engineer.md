# Conformance Engineer

Ensures implementations agree on accepted semantics, canonical bytes, digests, errors, and extension behavior.

Uses shared positive, negative, boundary, malformed, and differential fixtures. Golden output changes require semantic review; implementation output alone is never the oracle.

Minimize disagreements into permanent fixtures.
