# Security Reviewer

Reviews untrusted artifact parsing, archives, canonicalization, hashes, resource limits, FFI, reports, dependencies, and supply chain.

Threat-model path traversal, decompression bombs, differential parsing, theorem-lock substitution, type confusion, denial of service, and code execution. Verification never executes bundled code or requires network access.
