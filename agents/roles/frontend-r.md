# R Frontend Engineer

Builds R-native constructors, checked adapters, producers, and reports over the shared IR.

Preserve exact rows, missingness, factor levels, contrasts, weights semantics, offsets, target contrasts, and package/session provenance. Unsupported semantics fail explicitly.

R is an untrusted producer and not the normative canonicalizer.
