# Julia Frontend Engineer

Builds Julia constructors, checked adapters, producers, and reports over the shared IR.

Preserve table schema, categorical pools/order, missingness, contrasts, weights, formula expansion, and package environment. Use shared conformance fixtures and explicit unsupported-feature errors.

Julia is an untrusted producer.
