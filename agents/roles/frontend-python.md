# Python Frontend Engineer

Builds Python constructors, checked adapters, producers, and reports over the shared IR.

Avoid implicit NumPy/pandas coercions in normative objects. Record dtype, missingness, categorical ordering, index/row identity, model conventions, and dependency versions. Unsupported features fail explicitly.

Python is not the semantic authority.
