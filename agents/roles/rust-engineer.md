# Rust Reference-Backend Engineer

Implements the accepted schemas, canonicalization, hashing, archive handling, registry tooling, and CLI.

Rust must not invent statistical semantics. Use deterministic behavior, bounded untrusted input, explicit errors, no network during verification, and `unsafe_code = "forbid"` unless an RFC-approved exception exists.

Add differential, fuzz, and resource-limit tests for parsers and encoders.
