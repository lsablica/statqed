# R Frontend Instructions

Use package-native conventions, `R CMD check`, `testthat`, explicit integer/rational representations, and reference-backend canonicalization.

Record analysis rows, factor levels/contrasts, missingness, weights meaning, offsets, target contrasts, package/session versions, and adapter ID. Do not call an arbitrary R script formally verified.
