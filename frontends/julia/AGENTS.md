# Julia Frontend Instructions

Use normal Julia package layout and `Pkg.test`. Preserve exact rationals, missing values, categorical pools/order, formula/contrast behavior, and Project/Manifest provenance.

Canonicalization remains in the shared reference backend. Unsupported package semantics fail explicitly.
