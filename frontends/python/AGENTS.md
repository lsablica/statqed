# Python Frontend Instructions

Use a typed `src/` package, package-native tests/build, explicit `Fraction`/numeric tags, and reference-backend canonicalization.

Audit pandas/NumPy/model-package coercions, indices, dtypes, categories, missingness, and versioned adapter behavior. Do not treat ordinary Python numeric equality as semantic equivalence.
