# Language Frontends

Planned frontends:

- `frontends/r` — R package `statqed` (provisional);
- `frontends/python` — Python package `statqed` (provisional);
- `frontends/julia` — Julia package `StatQED.jl` (provisional).

Foundation tasks initialize package-native skeletons after schema v0. Native declarative mode comes first; checked model adapters come later.
