# Examples Scope Instructions

Scope: `examples/**`.

Examples must be fully reproducible, name exact locks, expose assumptions/nonclaims, and contain expected artifacts/digests. Do not use toy data to imply empirical validation. Every example includes deliberate failure or tampering demonstrations where relevant.
